import { asynchandler } from "../middleware/asyncHandler.js";import { User } from "../models/user.js";import * as projectServices from "../serrvices/projectServices.js";import * as notificationServices from "../serrvices/notificationService.js";import * as requestServices from "../serrvices/requestService.js";import * as fileServices from "../serrvices/fileServices.js";import { Project } from "../models/project.js";import { Notification } from "../models/notification.js";import * as userServices from "../serrvices/userServices.js";import { Supervisor } from "../models/supervisorrequest.js";import { sendemail } from "../serrvices/emailservice.js";import { generateRequestAcceptTemplate, generateRequestRejectedTemplate } from "../utiles/generateForgetPasswordEmailTemplate.js";export const getTeacherDashBoardStats = asynchandler(async (req, res) => {  const teacherId = req.user._id;  const totalPendingRequests = await Supervisor.countDocuments({    Supervisor: teacherId,    status: "pending",  });  const completedProjects = await Project.countDocuments({    Supervisor: teacherId,    status: "completed",  });  const recentNotification = await Notification.find({    user: teacherId,  })    .sort({ createdAt: -1 })    .limit(5);  const dashboardStats = {    totalPendingRequests,    completedProjects,    recentNotification,  };  res.status(200).json({    success: true,    message: "Dashboard Stats fetch",    data: { dashboardStats },  });});export const getRequests = asynchandler(async (req, res) => {  const { supervisor } = req.query;  const filters = {};  if (supervisor) filters.supervisor = supervisor;  const { request, total } = await requestServices.getAllRequests(filters);  const updatedRequests = await Promise.all(    request.map(async (reqobj) => {      const requestObj = reqobj.toObject ? reqobj.toObject() : reqobj;      if (requestObj?.student?._id) {        const latestProject = await Project.findOne({          student: requestObj.student._id,        })          .sort({ createdAt: -1 })          .lean();        return { ...requestObj, latestProject };      }      return requestObj;    }),  );  res.status(200).json({    success: true,    message: "Request fetch successfully",    data: {      requests: updatedRequests,      total,    },  });});export const accceptRequest = asynchandler(async (req, res) => {  const { requestId } = req.params;  const teacherId = req.user._id;  const request = await requestServices.acceptRequest(requestId, teacherId);  if (!request) {    return res.status(404).json({      success: false,      message: "Request not found",    });  }  await notificationServices.notifyUser(    request.student._id,    `Your Supervisor request has been accepted by ${req.user.name}`,    "approval",    "/students/stats",    "low",  );  const student = await User.findById(request.student._id);  const studentEmail = student.email;  const message = generateRequestAcceptTemplate(req.user.name);  await sendemail({    to: studentEmail,    subject: "FYP SYSTEM -Your supervisor Request Has Been Accepted",    message,  });  res.status(200).json({    success: true,    message: "Request Accepted successfully",    data: { request },  });});export const rejectRequests = asynchandler(async (req, res) => {  const { requestId } = req.params;  const teacherId = req.user._id;  const request = await requestServices.rejectRequest(requestId, teacherId);  if (!request) {    return res.status(404).json({      success: false,      message: "Request not found",    });  }  await notificationServices.notifyUser(    request.student._id,    `Your Supervisor request has been rejected by ${req.user.name}`,    "rejection",    "/students/stats",    "high",  );  const student = await User.findById(request.student._id);  const studentEmail = student.email;  const message = generateRequestRejectedTemplate(req.user.name);  await sendemail({    to: studentEmail,    subject: "FYP SYSTEM -Your supervisor Request Has Been rejected",    message,  });  res.status(200).json({    success: true,    message: "Request rejected successfully",    data: { request },  });});export const getAssignedStudents = asynchandler(async (req, res) => {  const teacherId = req.user._id;  const teacher = await User.findById(teacherId).populate("assigendStudents");  if (!teacher) {    return res.status(404).json({ success: false, message: "Teacher not found" });  }  const studentsWithProjects = await Promise.all(    teacher.assigendStudents.map(async (student) => {      const studentObj = student.toObject();      const latestProject = await Project.findOne({ student: student._id })        .sort({ createdAt: -1 })        .lean();      return { ...studentObj, latestProject };    })  );  res.status(200).json({    success: true,    message: "Assigned students fetched successfully",    data: { assignedStudents: studentsWithProjects },  });});export const getTeacherFiles = asynchandler(async (req, res) => {  const teacherId = req.user._id;  const teacher = await User.findById(teacherId);  const assignedStudentIds = teacher?.assigendStudents || [];  const projects = await Project.find({    $or: [{ supervisor: teacherId }, { student: { $in: assignedStudentIds } }]  }).populate("student", "name email");  let teacherFiles = [];  projects.forEach((proj) => {    if (proj.files && proj.files.length > 0) {      proj.files.forEach((file) => {        teacherFiles.push({          ...file.toObject(),          projectName: proj.title,          projectId: proj._id,          studentName: proj.student?.name || "Unknown",          studentEmail: proj.student?.email || "Unknown"        });      });    }  });  teacherFiles.sort((a, b) => new Date(b.uploadedAt) - new Date(a.uploadedAt));  res.status(200).json({    success: true,    message: "Teacher files fetched successfully",    data: { files: teacherFiles },  });});export const downloadTeacherFile = asynchandler(async (req, res) => {  const { projectId, fileId } = req.params;  const teacherId = req.user._id;  const project = await Project.findById(projectId);  if (!project) return res.status(404).json({ success: false, message: "Project not found" });  const teacher = await User.findById(teacherId);  const assignedStudentIds = teacher?.assigendStudents || [];  const isSupervisor = project.supervisor && project.supervisor.toString() === teacherId.toString();  const isAssigned = assignedStudentIds.includes(project.student.toString());  if (!isSupervisor && !isAssigned) {    return res.status(403).json({ success: false, message: "Not authorized to download file" });  }  const file = project.files.id(fileId);  if (!file) return res.status(404).json({ success: false, message: "file not found" });  fileServices.streamDownload(file.fileUrl, res, file.originalName);});