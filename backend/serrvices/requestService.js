import { Supervisor } from "../models/supervisorrequest.js";import { User } from "../models/user.js";import { Project } from "../models/project.js";export const createRequest = async (requestData) => {  const existingRequest = await Supervisor.findOne({    student: requestData.student,    supervisor: requestData.supervisor,    status: "pending",  });  if (existingRequest) {    const error = new Error(      "You have already sent a request to this supervisor.",    );    error.statusCode = 400;    throw error;  }  return await Supervisor.create(requestData);};export const getAllRequests = async (filters) => {  const request = await Supervisor.find(filters)    .populate("student", "name email")    .populate("supervisor", "name email")    .sort({ createdAt: -1 });  const total = await Supervisor.countDocuments(filters);  return { request, total };};export const acceptRequest = async (requestId, supervisorId) => {  const requests = await Supervisor.findById(requestId)    .populate("student", "name email supervisor project")    .populate("supervisor", "name email assignedStudents maxStudents");  if (!requests) throw new Error("Request not found");  if (requests.supervisor._id.toString() !== supervisorId.toString()) {    throw new Error("Not Authhorize to accept this request");  }  if (requests.status !== "pending") {    throw new Error("Request has already been processed");  }  requests.status = "accepted";  await requests.save();  await User.findByIdAndUpdate(supervisorId, {    $addToSet: { assigendStudents: requests.student._id }  });  await User.findByIdAndUpdate(requests.student._id, {    superVisor: supervisorId  });  await Project.updateMany(    { student: requests.student._id },    { $set: { supervisor: supervisorId } }  );  return await Supervisor.findById(requests._id)    .populate("student", "name email supervisor project")    .populate("supervisor", "name email assignedStudents maxStudents");};export const rejectRequest = async (requestId, supervisorId) => {  const requests = await Supervisor.findById(requestId)    .populate("student", "name email")    .populate("supervisor", "name email ");  if (!requests) throw new Error("Request not found");  if (requests.supervisor._id.toString() !== supervisorId.toString()) {    throw new Error("Not Authhorize to reject this request");  }  if (requests.status !== "pending") {    throw new Error("Request has already been processed");  }  requests.status = "rejected";  await requests.save();  return requests;};